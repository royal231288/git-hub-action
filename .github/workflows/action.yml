name: CI/CD pipe line
on: [push]
jobs:
  read_the_files: 
   runs-on: self-hosted
   steps:
    - name: sudo apt update
      run: sudo apt update
    - name: intall linux
      run: sudo apt install linux -y